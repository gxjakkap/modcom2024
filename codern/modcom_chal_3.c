#include <stdio.h>

int main(){
    int n, x, p;
    scanf("%d", &n);

    for (int i = 0; i < n; i++){
        x = 0;
        scanf("%d", &x);
        while (x != 0){
            
        }
    }

    return 0;
}